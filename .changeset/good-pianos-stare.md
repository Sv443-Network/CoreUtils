---
"@sv443-network/coreutils": minor
---

A second parameter of type `DataStoreDSOptions` is now also passed to the `filePath` function property in `FileStorageEngineOptions`, which allows for setting the file path based on encoding options.
